# Мобильная админ-панель — Шаг 3. Реализация адаптивных компонентов

## 1. Инфраструктура responsive-режимов
- **ResponsiveLayoutContext**
  - `breakpoint` вычисляется через `matchMedia` + `resize` (useEffect).
  - Экспорты: `useBreakpoint()`, `useResponsiveValue({ mobile, tablet, desktop })`, `useResponsiveLayout()`.
  - SSR/fallback: дефолт `desktop`, переключение после первого эффекта.
- **Сплит CSS**
  - Tailwind utility: `@layer components { .admin-container { @apply w-full max-w-5xl mx-auto px-4; } }`.
  - Добавить кастомные размеры (`xs: 360px`, `sm: 420px`).

## 2. Навигация
- **MobileAppBar** (`src/components/admin/navigation/MobileAppBar.tsx`)
  - Props: `title`, `onMenu`, `actions`, `searchEnabled`, `onSearch`.
  - Встраивает `NotificationButton`, `ProfileTrigger`.
- **MobileAdminDrawer** (`navigation/MobileAdminDrawer.tsx`)
  - Data-driven: массив `{ id, label, icon, badge, action }`.
  - Состояние открытости хранится в контексте `DrawerProvider`.
  - Использует `Sheet` (shadcn) + `ScrollArea`.
- **FloatingActionButton**
  - Универсальный компонент с variant (`primary`, `secondary`), поддержкой иконки/текста.
  - Отображается только на `isMobile`.

## 3. Карточные списки
- **ResponsiveList** (`lists/ResponsiveList.tsx`)
  - Props: `items`, `renderItem`, `empty`, `isLoading`, `onRefresh`.
  - Реализация: `PullToRefresh` (optional), `Virtuoso` для virtualization (mobile-friendly).
- **AdminCardBase** (`cards/AdminCardBase.tsx`)
  - ✅ Слоты: `leading`, `title`, `meta`, `children`, `footer`.
  - ✅ Поддержка состояния выбора (`selectable`, `selected`, `onSelectToggle`), hover/pressed стили.
- **Специализированные карточки**
  - ✅ `UserCard`, `SchoolCard`, `MasterClassCard`, `InvoiceCard`, `RequestCard`, `ChatDialogCard` — все построены на `AdminCardBase`, поддерживают selectable и тематические тона.

## 4. Фильтры и массовые действия
- ✅ **FilterChips** (`filters/FilterChips.tsx`) — список активных фильтров с `onRemove` и опциональным `onClearAll`.
- ✅ **FilterDrawer** (`filters/FilterDrawer.tsx`) — мобильный bottom sheet с кнопками «Сбросить» и «Применить».
- ✅ **SelectionManager** (`SelectionManagerProvider`) — Set-based выбор, методы `toggle`, `set`, `selectAll`, `clear`.
- ✅ **BulkActionBar** (`selection/BulkActionBar.tsx`) — фиксированная панель снизу с количеством выбранных и action-кнопками.

## 5. Модалки и bottom sheets
- **MobileDialog**
  - Обёртка над `DialogContent` с классами full-screen (`sm:max-w-none`, `rounded-none`).
  - Добавить `Close` icon и swipe-down gesture.
- **MobileBottomSheet**
  - Базируется на `Sheet`; props: `snapPoints` (например, `['35%', '75%']`).
  - План использования: детали счета, быстрая правка статуса, превью уведомлений.

## 6. Интеграция по разделам
- **Dashboard**
  - ✅ Drawer + MobileAppBar + FloatingActionButton интегрированы, карточки статистики переведены в одну колонку на мобильных.
  - ✅ Реализован mobile carousel быстрых ссылок и блок быстрых действий во вкладке Overview.
  - ✅ Collapsible секции статистики с умным дефолтом (mobile — top-3, desktop — раскрыто) и адаптивная сетка 1/2/3/4/5 колонок.
  - 🔜 Подготовить smoke-тесты и финальный чек-лист перед запуском шага 4.
- **Пользователи**
  - ✅ Мобильный режим: `ResponsiveList` + `UserCard` (десктоп остаётся на таблице).
  - Быстрые фильтры: роли, статус, наличие задолженности.
  - FAB: «Добавить пользователя» (открывает существующий модал).
- **Школы**
  - ✅ Мобильный режим: `ResponsiveList` + `SchoolCard` (метаданные, контакты, классы, действия).
  - Фильтры: город, количество классов, наличие активных заявок.
- **Мастер-классы**
  - ✅ Мобильный режим: `ResponsiveList` + `MasterClassCard` (дата, школа, исполнители, статус, суммы/участники).
  - Деталь: segmented control (Участники/Счета/Материалы/Заметки), карточки участников (`ParticipantCard`).
- **Счета**
  - ✅ Мобильный режим: `ResponsiveList` + `InvoiceCard`, quick actions для смены статуса/удаления (pending/cancelled).
  - ✅ Bottom sheet «Детали счета» (MobileBottomSheet) вместо полноэкранного модала.
- **Заявки**
  - ✅ `ResponsiveList` + `RequestCard`, изменение статуса и удаление через карточку.
- **Чат**
  - ✅ Лист диалогов на мобильных заменён `ResponsiveList` + `ChatDialogCard`, сообщения остаются в нижнем блоке (stack layout).
- **MasterClassDetails**
  - ✅ Мобильный режим: bottom sheet с вкладками (Обзор/Участники/Оплаты) и упрощёнными карточками действий.

## 7. Технические задачи
1. Реализовать `ResponsiveLayoutContext` + хуки. ✅
2. Собрать навигационные компоненты (AppBar, Drawer, FAB, Notifications). ✅ (AppBar, Drawer, FAB)
3. Создать базовые карточные компоненты и специализированные адаптеры. ✅
   - ✅ AdminCardBase + UserCard (PoC)
   - ✅ Интеграция UserCard + ResponsiveList во вкладку «Пользователи»
   - ✅ SchoolCard + мобильный список школ
   - ✅ MasterClassCard + мобильный список мастер-классов
   - ✅ InvoiceCard + мобильный список счетов
   - ✅ RequestCard + мобильный список заявок
   - ✅ ChatDialogCard + адаптация списка чатов
4. Внедрить `SelectionManager`, `BulkActionBar`, `FilterChips`, `FilterDrawer`. ✅
5. Обновить страницы (пользователи, школы, мастер-классы, счета, заявки, чат, Dashboard) на использование новых компонентов.
6. Переписать `MasterClassDetails` на segmented control и bottom sheets.
7. Добавить unit-тесты (`SelectionManager`, `useBreakpoint`) и сторибуки для карточек.
8. Smoke-тесты на устройствах (iPhone SE/11, Android Pixel 4, iPad mini).

## 8. Риски и контроль
- **Производительность**: следить за количеством re-render — использовать `memo`, virtualization.
- **Состояние фильтров**: убедиться, что старые ключи `React Query` пересозданы, кэш не сломан.
- **Модалки**: проверить совместимость с существующими `Dialog`/`Sheet`. При необходимости создать флаг `mobileOverride`.
- **Accessibility**: фокус-ловушки в bottom sheets, клавиатурная навигация (Tab/Shift+Tab), aria-labels для кнопок.

## 9. Следующие шаги
- Дизайн-макеты (Figma) уточнить/утвердить с заказчиком.
- Выполнить PoC для одного раздела (например, «Пользователи») до полного масштабирования.
- Оценить трудозатраты и разбить на мини-итерации (по разделам).
