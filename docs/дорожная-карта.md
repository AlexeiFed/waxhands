# Дорожная карта: адаптивная админ-панель (мобильная версия)

> **Принцип № 1:** мобильная версия не ломает существующую веб-функциональность. Все изменения — адаптивные, бизнес-логика и поведение остаются прежними.
>
> **Обязательные экраны:** пользователи, школы, мастер-классы (листинг + детальная), счета, чат, заявки. Остальные разделы — приоритет 2.

## 1. Исследование и проектирование
- 📋 **Аудит текущего UI**: собрать скриншоты/описания обязательных страниц, перечислить сценарии (просмотр, фильтры, CRUD, массовые действия).
- 📱 **Брейкпоинты**: 360–430 px (portrait), ≥ 640 px (landscape/PWA). Проверить отображение PWA-шапки.
- 🧭 **Навигация**: прототип гамбургер-меню + поиск + быстрые действия, сохранить доступность уведомлений/профиля.
- 🗺️ **User-flow**: low-fi прототипы (Figma) для каждого обязательного сценария (лист → деталь → редактирование → обратная навигация).

## 2. Навигация и шапка
- 🍔 **Hamburger Drawer**:
  - список разделов: Dashboard, Пользователи, Школы, Мастер-классы, Счета, Заявки, Чат, Настройки;
  - бейджи-счетчики (новые заявки, непрочитанные сообщения, неоплаченные счета);
  - быстрые действия (создать пользователя, школу, мастер-класс).
- 🧠 **Context toolbar**:
  - AppBar: кнопка меню, логотип, глобальный поиск (иконка), уведомления (badge), аватар администратора;
  - режим списка: заголовок раздела, кнопка «создать», фильтры/сортировка;
  - режим детали: «назад», «редактировать», «поделиться», «действия» (kebab menu) + статусный бейдж.
- 📱 **Bottom actions**:
  - рабочий FAB (плавающая кнопка) для создания entities на длинных списках;
  - опциональный bottom-sheet с быстрыми фильтрами или массовыми действиями (когда выбран хотя бы один элемент).
- 🔔 **Уведомления**:
  - отдельный экран/модалка с лентой событий (Robokassa, заявки, чат);
  - свайп для отметки «прочитано», группировка по типу события.

## 3. Главный экран (Dashboard)
- 📊 **Карточки**: вертикальный scroll, карточки full-width, collapsible-виджеты для статистики.
- 🔄 **Лента задач**: превью ключевых списков (мастер-классы, заявки) как swipeable carousel с цветовым кодированием статусов.

## 4. Списки и таблицы
- 📑 **Таблицы → карточки**:
  - заголовок: ключевые поля (ФИО/название/дата);
  - подзаголовок: контакт, школа, статус (цветной бейдж);
  - контент: краткий summary (сумма счета, количество участников, теги);
  - CTA: основное действие (открыть, позвонить, оплатить) + вторичные в контекстном меню.
- ☑️ **Массовые действия**:
  - long-press или чекбокс -> включается режим выбора;
  - нижняя панель: «Удалить», «Назначить», «Экспорт», «Изменить статус», счётчик выбранных;
  - по окончании — swipe down или кнопка «Отмена».
- 🔍 **Фильтры и поиск**:
  - верхний AppBar: быстрые фильтры (статус, дата, роль) с горизонтальным scroll;
  - расширенные фильтры: bottom sheet / Drawer (checkbox, selects, date-range);
  - сохранение последнего набора, пресеты («Только новые», «Просроченные счета»).
- 💬 **Взаимодействие с карточкой**:
  - свайп влево/вправо — быстрые действия (архив, отметить выполненным);
  - tap — открытие детали / bottom sheet с расширенной информацией.

## 5. Формы и модалки
- 📋 **Многошаговые формы**: пошаговый wizard с прогресс-индикатором, крупные CTA.
- 🪟 **Modal strategy**: bottom-sheet для быстрых правок, полноэкранные модалки для крупных форм, sticky bottom CTA.
- ✍️ **Input UX**: крупные поля, floating labels, авто-скролл к полю с ошибкой.

## 6. Состояния и коммуникации
- 🚫 **Empty/Error**: понятные тексты + CTA «создать/повторить».
- 🔁 **Skeleton / Pull-to-refresh**: для обязательных списков (пользователи, заявки, счета).
- 📬 **Уведомления**: опционально push/e-mail для критичных событий (post-MVP).

## 7. Технологическая реализация
- 🧱 **Layout**: Tailwind `grid-cols-1`, `gap-4`, `max-w` контейнеры, медиазапросы `@media (min-width: 768px)` для возврата к desktop.
- 🛠️ **Компоненты**: mobile-friendly версии `TabsList`, `Table`, `FilterBar`, общий `ResponsiveContainer` (breakpoint в контексте).
- ♻️ **Состояние**: переиспользовать React Query; учесть новые ключи для карточных списков, avoidance двойных запросов.
- 📦 **Performance**: lazy loading разделов, оптимизация бандла, кэширование API, Lighthouse на mobile.

## 8. Тестирование
- ✅ **Device matrix**: Chrome DevTools, iOS Safari, Android WebView/PWA.
- 🧪 **Checklist**: навигация без ховеров, читаемость > 12 pt, доступность (ARIA, focus-trap, контраст), корректное поведение PWA.
- 📊 **Аналитика**: Core Web Vitals, мониторинг ошибок (Sentry/LogRocket).

## 9. Раскатка
- 🗂️ **Feature flag**: `ENABLE_MOBILE_ADMIN` → поэтапный rollout (внутренние пользователи → ограниченная аудитория → все).
- 🔄 **Документация**: обновить `docs/project.md`, инструкции для админов, onboarding.
- 📣 **Коммуникация**: объявление релиза, собрать feedback, план итераций.

## 10. Шаг 3 — реализация адаптивных компонентов
- 🧩 **Каркас**:
  - внедрить `ResponsiveLayout` HOC/контекст, возвращающий варианты `isMobile`, `isTablet`, `isDesktop`;
  - адаптивная сетка для всего админ-dashboard (flex-col до `md`, двухколоночная после `lg`).
- 📱 **Навигация**:
  - реализовать `MobileAppBar` с прокидываемыми actions, глобальным поиском, badge уведомлений;
  - Drawer меню (`MobileAdminDrawer`) c секциями и бейджами, управление через контекст;
  - `FloatingActionButton` компонент с поддержкой разных иконок/текстов.
- 🗂️ **Карточные списки**:
  - создать `ResponsiveList` (virtual scroll + skeleton) и `AdminCard` базовый шаблон с зонами header/body/footer;
  - адаптеры для конкретных разделов (`UserCard`, `SchoolCard`, `MasterClassCard`, `InvoiceCard`, `RequestCard`, `ChatDialogCard`).
- 🔍 **Фильтры**:
  - компонент `FilterChips` (горизонтальный scroll) + `FilterDrawer`/`FilterBottomSheet` с поддержкой сохранённых пресетов;
  - связываем с `AdminFiltersContext`, добавляем методы `savePreset`, `loadPreset`.
- ☑️ **Массовые действия**:
  - контекст `SelectionManager` (выбранные id, onSelect, onClear);
  - `BulkActionBar` (появляется fixed bottom), поддержка адаптивных кнопок/иконок.
- 🪟 **Модалки/Bottom sheets**:
  - переиспользовать `Dialog` + обёртку `MobileDialog` (full-screen, swipe-to-close);
  - `MobileBottomSheet` на базе `Sheet` из shadcn/ui.
- 🔄 **Интеграция**:
  - Dashboard: переподключить табы → Drawer + FAB, карточки статистики вертикально;
  - Пользователи/Школы/Счета/Заявки: заменить `Table` на карточный список, фильтры перевести на Drawer;
  - MasterClassDetails: адаптивный таб-набор (segmented control), карточки участников и оплат;
  - Чат: режим «list + detail» → переключение через Drawer/stack навигации.
- ✅ Реализовано: ResponsiveLayout + MobileAppBar/MobileAdminDrawer/FloatingActionButton, MobileBottomSheet, ResponsiveList + карточки (User/School/MasterClass/Invoice/Request/Chat), SelectionManager, FilterChips/FilterDrawer, overview-carousel быстрых ссылок и мобильные quick actions.
- 🔜 В работе: Подготовить smoke-тесты (mobile/desktop) перед запуском шага 4.
- 🧪 **Контроль качества**:
  - юнит-тесты для `SelectionManager`, `ResponsiveLayout`;
  - визуальные скриншоты (Storybook) для основных карточек.


