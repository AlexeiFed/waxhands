# Настройка доступа к серверу timeweb.cloud

## Проблема
Не удается войти на сервер под пользователем `waxhands` для переноса папки `production`.

## Решение 1: Через панель управления timeweb.cloud

### Шаг 1: Вход в панель управления
1. Откройте https://timeweb.cloud
2. Войдите в свой аккаунт
3. Перейдите в раздел "Серверы" → ваш сервер

### Шаг 2: Доступ через веб-консоль
1. В панели сервера найдите "Консоль" или "Web SSH"
2. Нажмите "Открыть консоль"
3. Войдите под пользователем `waxhands`:
   ```bash
   su - waxhands
   # или
   sudo su - waxhands
   ```

### Шаг 3: Проверка прав доступа
```bash
# Проверить текущего пользователя
whoami

# Проверить домашнюю директорию
echo $HOME

# Проверить права на папки
ls -la /var/www/
```

## Решение 2: Настройка SSH доступа

### Шаг 1: Создание SSH ключа на локальной машине
```bash
# В PowerShell на Windows
ssh-keygen -t rsa -b 4096 -C "waxhands@waxhands.ru"
# Сохранить в: C:\Users\[ваше_имя]\.ssh\id_rsa_waxhands
```

### Шаг 2: Копирование ключа на сервер
```bash
# Через веб-консоль timeweb.cloud
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Создать файл authorized_keys
touch ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# Добавить ваш публичный ключ
echo "ssh-rsa AAAAB3NzaC1yc2E... ваш_ключ" >> ~/.ssh/authorized_keys
```

### Шаг 3: Подключение по SSH
```bash
# С локальной машины
ssh -i C:\Users\[ваше_имя]\.ssh\id_rsa_waxhands waxhands@[IP_сервера]
```

## Решение 3: Через SFTP/SCP

### Шаг 1: Использование WinSCP
1. Скачайте WinSCP: https://winscp.net/
2. Настройте подключение:
   - Протокол: SFTP
   - Хост: IP вашего сервера
   - Порт: 22
   - Имя пользователя: waxhands
   - Пароль: ваш_пароль

### Шаг 2: Перенос файлов
1. Подключитесь к серверу
2. Перейдите в `/var/www/waxhands.ru/`
3. Скопируйте папку `production` из локальной машины

## Решение 4: Через Git (рекомендуется)

### Шаг 1: Настройка Git на сервере
```bash
# В веб-консоли timeweb.cloud
cd /var/www/waxhands.ru/
git init
git remote add origin https://github.com/[ваш_репозиторий].git
```

### Шаг 2: Клонирование/обновление
```bash
# Клонировать репозиторий
git clone https://github.com/[ваш_репозиторий].git .

# Или обновить существующий
git pull origin main
```

## Проверка работоспособности

### Шаг 1: Тест backend
```bash
cd /var/www/waxhands.ru/backend
node index.js
# Должен запуститься без ошибок
```

### Шаг 2: Проверка портов
```bash
# Проверить какие порты слушает backend
netstat -tlnp | grep :3001
```

### Шаг 3: Проверка логов
```bash
# Если есть ошибки, проверить логи
tail -f /var/log/syslog | grep waxhands
```

## Частые проблемы и решения

### Проблема: Permission denied
```bash
# Исправить права доступа
sudo chown -R waxhands:waxhands /var/www/waxhands.ru/
sudo chmod -R 755 /var/www/waxhands.ru/
```

### Проблема: Порт занят
```bash
# Найти процесс использующий порт
sudo lsof -i :3001
# Остановить процесс
sudo kill -9 [PID]
```

### Проблема: База данных недоступна
```bash
# Проверить статус PostgreSQL
sudo systemctl status postgresql
# Перезапустить если нужно
sudo systemctl restart postgresql
```

## Контакты поддержки

Если ничего не помогает:
1. Обратитесь в поддержку timeweb.cloud
2. Укажите номер вашего сервера
3. Опишите проблему с доступом под пользователем waxhands

## Готово! 🚀

После успешного входа и переноса файлов следуйте инструкции из `SERVER_DEPLOYMENT.md`
