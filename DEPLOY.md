# 🚀 Руководство по развертыванию Wax Hands PWA

> Автоматизированные скрипты для безопасного деплоя frontend и backend на сервер waxhands.ru

## 📋 Содержание

- [Структура проекта на сервере](#структура-проекта-на-сервере)
- [Предварительные требования](#предварительные-требования)
- [Скрипты деплоя](#скрипты-деплоя)
- [Пошаговые инструкции](#пошаговые-инструкции)
- [Диагностика и откат](#диагностика-и-откат)
- [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## 🏗️ Структура проекта на сервере

### Сервер: timeweb.cloud
- **IP**: 147.45.161.83
- **Домен**: waxhands.ru
- **SSH**: root@147.45.161.83

### Структура файлов

```
/var/www/waxhands-app/
├── frontend/              # Frontend приложения (root для Nginx)
│   ├── assets/           # JS, CSS, изображения
│   ├── icons/            # PWA иконки
│   ├── index.html        # Главная страница
│   ├── manifest.json     # PWA манифест
│   └── sw.js             # Service Worker
│
├── backend/              # Backend приложения
│   ├── dist/            # Скомпилированные файлы (рабочая версия)
│   ├── dist.backup.*    # Автоматические резервные копии
│   ├── uploads/         # Загруженные пользователями файлы
│   ├── .env             # Переменные окружения
│   └── node_modules/    # Зависимости
│
└── uploads/              # Общие uploads (доступны через /uploads/)
```

### Nginx конфигурация

- **Root для frontend**: `/var/www/waxhands-app/frontend`
- **API проксирование**: `/api/*` → `http://localhost:3001`
- **WebSocket**: `/ws` → `http://localhost:3001`
- **Uploads**: `/uploads/` → `/var/www/waxhands-app/uploads/`
- **SSL**: Автоматическое перенаправление HTTP → HTTPS

### PM2 процессы

```bash
pm2 list
# ┌────┬──────────────────┬─────────┬──────────┐
# │ id │ name             │ status  │ uptime   │
# ├────┼──────────────────┼─────────┼──────────┤
# │ 1  │ waxhands-backend │ online  │ 39D      │
# └────┴──────────────────┴─────────┴──────────┘
```

---

## ✅ Предварительные требования

### 1. Локальная машина (Windows)

- [x] **PowerShell 5.1+** (проверить: `$PSVersionTable.PSVersion`)
- [x] **Node.js 18+** и npm (проверить: `node -v`)
- [x] **SSH клиент** (встроен в Windows 10+)
- [x] **Git** (опционально)
- [x] **tar** (встроен в Windows 10+)

### 2. SSH доступ к серверу

Убедитесь, что SSH ключ настроен:

```powershell
# Тест подключения
ssh root@147.45.161.83 "echo 'Connection OK'"
```

Если требуется пароль при каждом подключении, настройте SSH ключи:

```powershell
# Генерация ключа (если нет)
ssh-keygen -t rsa -b 4096

# Копирование на сервер
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@147.45.161.83 "cat >> ~/.ssh/authorized_keys"
```

### 3. Структура проекта

Скрипты должны запускаться **из корня проекта**:

```
waxhands-playful-pwa/
├── deploy-frontend-v2.ps1  ← Новый скрипт для frontend
├── deploy-backend-v2.ps1   ← Новый скрипт для backend
├── backend/                ← Backend код
│   └── dist/              (создается при сборке)
├── dist/                   ← Frontend dist (создается при сборке)
└── package.json
```

---

## 📦 Скрипты деплоя

### `deploy-frontend-v2.ps1` - Деплой Frontend

**Что делает:**

1. ✅ Очищает кэш и старые сборки
2. 🔨 Собирает frontend (`npm run build`)
3. 🔍 Проверяет созданные файлы и их размер
4. 📦 Создает tar.gz архив с timestamp
5. ⬆️ Загружает на сервер через SCP
6. 🗑️ **Полностью очищает** `/var/www/waxhands-app/frontend/`
7. 📂 Распаковывает новые файлы
8. 🔄 Перезагружает Nginx
9. 🧹 Удаляет старые локальные архивы

**Использование:**

```powershell
# Из корня проекта
.\deploy-frontend-v2.ps1
```

**Ожидаемый вывод:**

```
╔════════════════════════════════════════════════════════╗
║        FRONTEND DEPLOYMENT для waxhands.ru            ║
╚════════════════════════════════════════════════════════╝

🗑️  Шаг 1/8: Очистка кэша и старых сборок
  ✓ Кэш очищен

🔨 Шаг 2/8: Сборка frontend
  ✓ Сборка успешна

📦 Шаг 3/8: Проверка созданных файлов
  → Последний JS файл: index-ABC123.js
  → Размер: 2.45 MB
  ✓ Файлы проверены и валидны

...

╔════════════════════════════════════════════════════════╗
║             DEPLOYMENT ЗАВЕРШЕН УСПЕШНО!              ║
╚════════════════════════════════════════════════════════╝

⚠️  ВАЖНО: Нажмите Ctrl+F5 в браузере для обновления!
```

---

### `deploy-backend-v2.ps1` - Деплой Backend

**Что делает:**

1. 📂 Переходит в папку `backend/`
2. 🗑️ Очищает старую сборку
3. 🔨 Собирает backend (`npm run build`)
4. 🔍 Проверяет ключевые файлы (index.js, websocket-server.js)
5. 📦 Создает tar.gz архив с timestamp
6. ⬆️ Загружает на сервер через SCP
7. 💾 **Создает резервную копию** `dist.backup.TIMESTAMP`
8. 📂 Распаковывает новый backend в `dist/`
9. 🔄 Перезапускает PM2 процесс `waxhands-backend`
10. ✅ Проверяет работоспособность API
11. 🧹 Удаляет старые резервные копии (оставляет 5 последних)

**Использование:**

```powershell
# Из корня проекта
.\deploy-backend-v2.ps1
```

**Ожидаемый вывод:**

```
╔════════════════════════════════════════════════════════╗
║         BACKEND DEPLOYMENT для waxhands.ru            ║
╚════════════════════════════════════════════════════════╝

📂 Шаг 1/9: Переход в директорию backend
  ✓ Текущая папка: C:\projects\waxhands-playful-pwa\backend

🔨 Шаг 3/9: Сборка backend
  ✓ Сборка успешна

...

🔍 Шаг 9/9: Проверка работоспособности
  → HTTP статус: 200
  ✓ Backend отвечает на запросы

╔════════════════════════════════════════════════════════╗
║             DEPLOYMENT ЗАВЕРШЕН УСПЕШНО!              ║
╚════════════════════════════════════════════════════════╝

💡 Полезные команды:
   ssh root@147.45.161.83 'pm2 logs waxhands-backend'
   ssh root@147.45.161.83 'pm2 restart waxhands-backend'
```

---

## 📖 Пошаговые инструкции

### Сценарий 1: Обновление только Frontend

**Когда использовать:** Изменения в UI, стилях, компонентах React

```powershell
# 1. Убедитесь, что находитесь в корне проекта
cd C:\projects\waxhands-playful-pwa

# 2. Запустите скрипт
.\deploy-frontend-v2.ps1

# 3. После успешного деплоя откройте браузер и нажмите Ctrl+F5
```

**Время выполнения:** ~2-4 минуты

---

### Сценарий 2: Обновление только Backend

**Когда использовать:** Изменения в API, базе данных, бизнес-логике

```powershell
# 1. Убедитесь, что находитесь в корне проекта
cd C:\projects\waxhands-playful-pwa

# 2. Запустите скрипт
.\deploy-backend-v2.ps1

# 3. Проверьте логи
ssh root@147.45.161.83 "pm2 logs waxhands-backend --lines 20"
```

**Время выполнения:** ~2-3 минуты

---

### Сценарий 3: Полное обновление (Frontend + Backend)

**Когда использовать:** Комплексные изменения, затрагивающие и UI, и API

```powershell
# 1. Сначала обновите Backend (чтобы API был готов)
.\deploy-backend-v2.ps1

# 2. Дождитесь успешного завершения

# 3. Затем обновите Frontend
.\deploy-frontend-v2.ps1

# 4. Проверьте работоспособность
# Откройте https://waxhands.ru и нажмите Ctrl+F5
```

**Время выполнения:** ~5-7 минут

---

## 🔧 Диагностика и откат

### Проверка статуса сервера

```bash
# Подключение к серверу
ssh root@147.45.161.83

# Проверка PM2 процессов
pm2 status
pm2 show waxhands-backend

# Логи backend
pm2 logs waxhands-backend --lines 50

# Проверка Nginx
systemctl status nginx
tail -f /var/log/nginx/waxhands-error.log

# Проверка доступного места
df -h

# Список файлов frontend
ls -lh /var/www/waxhands-app/frontend/assets/

# Список резервных копий backend
ls -lt /var/www/waxhands-app/backend/dist.backup.*
```

---

### Откат Frontend к предыдущей версии

⚠️ **Frontend не имеет автоматических резервных копий!** 

Если обновление frontend сломало приложение, есть 2 варианта:

**Вариант 1: Откат через Git (рекомендуется)**

```powershell
# Локально откатитесь к предыдущему коммиту
git log --oneline -5
git checkout <commit-hash>

# Разверните старую версию
.\deploy-frontend-v2.ps1

# Вернитесь к текущей ветке
git checkout main
```

**Вариант 2: Ручная загрузка старого архива**

```powershell
# Найти локальные архивы
Get-ChildItem -Filter "frontend-update-*.tar.gz" | Sort-Object LastWriteTime -Descending

# Загрузить старый архив
scp frontend-update-СТАРЫЙ_TIMESTAMP.tar.gz root@147.45.161.83:/tmp/frontend-rollback.tar.gz

# На сервере
ssh root@147.45.161.83
cd /var/www/waxhands-app/frontend
rm -rf *
tar -xzf /tmp/frontend-rollback.tar.gz
systemctl reload nginx
```

---

### Откат Backend к предыдущей версии

✅ **Backend автоматически создает резервные копии!**

**Автоматический откат:**

```bash
ssh root@147.45.161.83

# Посмотреть доступные бэкапы
ls -lt /var/www/waxhands-app/backend/dist.backup.*

# Откат к последнему бэкапу
cd /var/www/waxhands-app/backend
LAST_BACKUP=$(ls -dt dist.backup.* | head -1)
echo "Откат к: $LAST_BACKUP"
rm -rf dist
cp -r $LAST_BACKUP dist
pm2 restart waxhands-backend

# Проверка
pm2 logs waxhands-backend --lines 20
```

**Откат к конкретной версии:**

```bash
cd /var/www/waxhands-app/backend

# Выбрать нужную версию
ls -lt dist.backup.*

# Откат
rm -rf dist
cp -r dist.backup.20251126-051510 dist
pm2 restart waxhands-backend
```

---

### Проверка работоспособности после деплоя

```bash
# 1. API отвечает
curl -I https://waxhands.ru/api/health

# 2. Frontend загружается
curl -I https://waxhands.ru

# 3. PM2 процесс работает
ssh root@147.45.161.83 "pm2 status"

# 4. Nginx работает
ssh root@147.45.161.83 "systemctl status nginx"

# 5. Нет ошибок в логах
ssh root@147.45.161.83 "pm2 logs waxhands-backend --lines 50 --nostream"
ssh root@147.45.161.83 "tail -50 /var/log/nginx/waxhands-error.log"
```

---

## ❓ Часто задаваемые вопросы

### Q: Ошибка "Build failed" при сборке Frontend

**A:** Проверьте код на наличие ошибок TypeScript/ESLint:

```powershell
# Локальная проверка
npm run build

# Если есть ошибки - исправьте их
npm run lint
```

---

### Q: Ошибка "Cannot connect to server" при загрузке

**A:** Проверьте SSH подключение:

```powershell
# Тест подключения
ssh root@147.45.161.83 "echo 'OK'"

# Проверка SSH ключей
ssh-add -l

# Добавление ключа
ssh-add $env:USERPROFILE\.ssh\id_rsa
```

---

### Q: Frontend обновился, но браузер показывает старую версию

**A:** Это проблема кэширования браузера:

1. **Ctrl+F5** (принудительное обновление)
2. Очистите кэш браузера (Ctrl+Shift+Del)
3. Откройте в режиме инкогнито
4. Проверьте Network tab в DevTools - должен загружаться новый JS файл

---

### Q: Backend перезапустился, но не работает

**A:** Проверьте логи и статус:

```bash
ssh root@147.45.161.83

# Логи PM2
pm2 logs waxhands-backend --lines 100

# Статус
pm2 show waxhands-backend

# Перезапуск
pm2 restart waxhands-backend

# Полный перезапуск
pm2 stop waxhands-backend
pm2 start waxhands-backend
pm2 save
```

---

### Q: Ошибка "disk full" на сервере

**A:** Очистите старые бэкапы и архивы:

```bash
ssh root@147.45.161.83

# Проверка места
df -h

# Очистка старых бэкапов backend (оставить 3 последних)
cd /var/www/waxhands-app/backend
ls -dt dist.backup.* | tail -n +4 | xargs rm -rf

# Очистка временных файлов
rm -f /tmp/frontend-update.tar.gz
rm -f /tmp/backend-update.tar.gz

# Очистка логов
pm2 flush
```

---

### Q: Как узнать, какая версия сейчас на сервере?

**A:** Проверьте даты файлов и PM2:

```bash
ssh root@147.45.161.83

# Frontend - дата последнего обновления
ls -lh /var/www/waxhands-app/frontend/assets/index-*.js

# Backend - uptime PM2
pm2 show waxhands-backend | grep -E '(uptime|restarts)'

# Последний бэкап backend
ls -lt /var/www/waxhands-app/backend/dist.backup.* | head -1
```

---

### Q: Можно ли откатить изменения автоматически?

**A:** Для Backend - да (см. раздел "Откат Backend"). Для Frontend - используйте Git:

```powershell
# Откат локально
git log --oneline -10
git revert <commit-hash>

# Деплой отката
.\deploy-frontend-v2.ps1
```

---

## 🛡️ Лучшие практики

### ✅ DO (Рекомендуется)

- ✅ Всегда тестируйте локально перед деплоем: `npm run dev`
- ✅ Делайте Git commit перед деплоем
- ✅ Обновляйте Backend перед Frontend при комплексных изменениях
- ✅ Проверяйте логи после деплоя
- ✅ Сохраняйте локальные архивы (скрипты оставляют 3 последних)
- ✅ Документируйте изменения в `/docs/changelog.md`

### ❌ DON'T (Не рекомендуется)

- ❌ Не редактируйте файлы напрямую на сервере
- ❌ Не удаляйте все бэкапы backend сразу
- ❌ Не деплойте в часы пик (обед 12-14, вечер 18-20)
- ❌ Не пропускайте проверку после деплоя
- ❌ Не запускайте оба скрипта одновременно

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи выше
2. Изучите раздел "Диагностика и откат"
3. Проверьте документацию в `/docs/project.md`
4. Проверьте `.cursorrules` для специфичных правил проекта

---

**Последнее обновление:** 2026-01-19  
**Версия документа:** 2.0  
**Автор:** AI Assistant  
**Проект:** Wax Hands PWA  

