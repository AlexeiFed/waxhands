# Инструкция по тестированию функциональности развертывания текста

## 📝 Что было реализовано

### 1. Компонент ExpandableText
- **Файл**: `src/components/ui/expandable-text.tsx`
- **Функциональность**: Универсальный компонент для развертывания/сворачивания длинного текста
- **Настраиваемые параметры**: maxLength, className, buttonClassName, showButtonText

### 2. Обновленные компоненты лендинга
- **ServicesSection**: Описания услуг и стилей ручек
- **AboutSection**: Описания студии и безопасности
- **CTASection**: Тексты в карточках преимуществ

## 🧪 Как протестировать

### Тестирование на десктопе

1. **Откройте лендинг**: https://waxhands.ru
2. **Прокрутите до секции услуг** - найдите карточки с описаниями
3. **Проверьте функциональность**:
   - Длинные тексты должны быть обрезаны с "..."
   - Должна появиться кнопка с иконкой стрелки вниз
   - При клике текст должен развернуться полностью
   - Кнопка должна измениться на стрелку вверх
   - При повторном клике текст должен свернуться

### Тестирование на мобильных устройствах

1. **Откройте лендинг на мобильном** устройстве
2. **Проверьте адаптивность**:
   - Тексты должны корректно обрезаться
   - Кнопки должны быть достаточно большими для касания
   - Анимации должны работать плавно

### Конкретные места для проверки

#### ServicesSection (Секция услуг)
- **Описания услуг**: `service.shortDescription` и `service.fullDescription`
- **Описания стилей**: `style.fullDescription` в карточках вариантов ручек
- **Параметры**: maxLength=120 для описаний услуг, maxLength=100 для стилей

#### AboutSection (Секция "О нас")
- **Описание студии**: "Что мы делаем"
- **Описание безопасности**: `displayContent.safety_description`
- **Параметры**: maxLength=120 для описания студии, maxLength=150 для безопасности

#### CTASection (Секция призыва к действию)
- **Карточка "Для всей семьи"**: Описание преимущества
- **Карточка "Быстро и просто"**: Описание процесса
- **Параметры**: maxLength=80 для компактных карточек

## 🎨 Настройки компонента

### Основные параметры
```typescript
<ExpandableText
    text="Длинный текст..."
    maxLength={120}                    // Максимальная длина свернутого текста
    className="text-lg text-gray-700"  // Стили для текста
    buttonClassName="text-orange-600"  // Стили для кнопки
    showButtonText={false}             // Показывать текст на кнопке
    expandedButtonText="Свернуть"      // Текст кнопки в развернутом состоянии
    collapsedButtonText="Развернуть"   // Текст кнопки в свернутом состоянии
/>
```

### Цветовые схемы
- **Оранжевая**: `text-orange-600` (основные описания)
- **Фиолетовая**: `text-purple-600` (стили ручек)
- **Зеленая**: `text-green-600` (безопасность)
- **Синяя**: `text-blue-600` (преимущества)

## ✅ Критерии успешного тестирования

### Функциональность
1. **Тексты обрезаются** при превышении maxLength
2. **Кнопка появляется** только для длинных текстов
3. **Развертывание работает** корректно
4. **Сворачивание работает** корректно
5. **Анимации плавные** и не лагают

### Адаптивность
1. **На мобильных устройствах** кнопки достаточно большие
2. **Тексты не выходят** за границы карточек
3. **Кнопки не перекрываются** с другим контентом
4. **Шрифты читаемы** на всех размерах экрана

### Производительность
1. **Нет задержек** при развертывании
2. **Плавные переходы** между состояниями
3. **Не влияет на загрузку** страницы

## 🐛 Возможные проблемы и решения

### Текст не обрезается
- Проверить, что `text.length > maxLength`
- Убедиться, что текст не пустой

### Кнопка не появляется
- Проверить, что `shouldShowButton` установлен в true
- Убедиться, что `useEffect` срабатывает

### Стили не применяются
- Проверить правильность CSS классов
- Убедиться, что Tailwind CSS загружен

### На мобильных не работает
- Проверить размеры кнопок (минимум 44px для касания)
- Убедиться, что нет перекрытий с другими элементами

## 🔧 Настройка параметров

### Для разных типов контента
```typescript
// Короткие описания (карточки преимуществ)
maxLength={80}

// Обычные описания (услуги, о нас)
maxLength={120}

// Длинные описания (безопасность, подробности)
maxLength={150}

// Очень длинные описания (полные тексты)
maxLength={200}
```

### Размеры кнопок
```typescript
// Маленькие кнопки (для компактных карточек)
buttonClassName="text-xs"

// Обычные кнопки (стандартный размер)
buttonClassName="text-sm"

// Большие кнопки (для важного контента)
buttonClassName="text-base"
```

## 📱 Мобильная оптимизация

### Рекомендуемые настройки для мобильных
- **minLength**: 60-80 символов (короче чем на десктопе)
- **buttonClassName**: `text-sm` или больше для удобства касания
- **showButtonText**: `true` для ясности на мобильных

### Проверка на разных устройствах
- **iPhone SE** (375px): Самый узкий экран
- **iPhone 12** (390px): Стандартный мобильный
- **iPad** (768px): Планшет
- **Desktop** (1024px+): Десктоп

## 🎯 Результат

После успешного тестирования:
- ✅ Длинные тексты в карточках лендинга аккуратно обрезаются
- ✅ Пользователи могут развернуть текст при необходимости
- ✅ Интерфейс остается чистым и не перегруженным
- ✅ Функциональность работает на всех устройствах
- ✅ Производительность не страдает



