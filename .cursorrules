# .cursorrules - Правила разработки Wax Hands PWA
Ты мой помощник в разработке программного обеспечения. Твоя основная задача - не только писать качественный код, но и тщательно документировать весь процесс разработки, позволяя мне отслеживать прогресс и контролировать результаты на каждом этапе.

## 🖥️ Информация о сервере

### Доступ к серверу
- **Сервер**: timeweb.cloud
- **IP**: 147.45.161.83
- **Домен**: waxhands.ru
- **SSH**: root@147.45.161.83
- **Путь**: /var/www/waxhands-app/

### Домен и SSL
- **Основной домен**: waxhands.ru
- **SSL сертификат**: `/etc/ssl/certs/waxhands.ru.crt`
- **SSL ключ**: `/etc/ssl/private/waxhands.ru.key`
- **Nginx конфигурация**: `/etc/nginx/sites-enabled/waxhands-app`
- **Автоматическое перенаправление**: HTTP → HTTPS, www → основной домен

### WebSocket сервер
- **Файл**: `websocket-server-simple.js` в корне проекта
- **Порт**: 3002 (отдельный от основного backend)
- **URL**: `wss://waxhands.ru/ws`
- **Запуск**: `node websocket-server-simple.js &`
- **Проверка**: `netstat -tlnp | grep 3002`
- **Статус**: HTTP 426 (Upgrade Required) = работает корректно

### Структура на сервере
```
/var/www/waxhands-app/
├── backend/           # Бэкенд приложения
│   ├── dist/         # Скомпилированные файлы
│   ├── dist.backup/  # Резервная копия
│   ├── uploads/      # Загруженные файлы
│   └── .env          # Переменные окружения
├── frontend/          # Фронтенд приложения
│   ├── assets/       # Статические ресурсы
│   ├── uploads/      # Загруженные файлы (images/)
│   └── .env          # Переменные окружения
├── websocket-server-simple.js  # Отдельный WebSocket сервер
└── .env               # Общие переменные окружения
```

### Процесс обновления кода

#### Backend
1. **Локальная сборка**: `npm run build` в папке backend
2. **Создание архива**: PowerShell скрипт `update-server.ps1`
3. **Загрузка на сервер**: `scp backend-update.zip root@147.45.161.83:/tmp/`
4. **Обновление на сервере**:
   ```bash
   ssh root@147.45.161.83
   cd /var/www/waxhands-app/backend
   cp -r dist dist.backup
   rm -rf dist
   unzip /tmp/backend-update.zip -d .
   pm2 restart waxhands-backend
   ```

#### Frontend
1. **Очистка кэша и пересборка**:
   ```powershell
   # Очистка всех кэшей
   Remove-Item -Recurse -Force dist
   Remove-Item -Force backend\*.tsbuildinfo
   Remove-Item -Force *.tsbuildinfo
   
   # Пересборка
   npm run build
   ```

2. **Проверка новых файлов**:
   ```powershell
   # Убедиться, что создался новый JS файл
   dir dist\assets\index-*.js
   # Должен быть новый файл, НЕ старый index-BEWE0pAD.js
   ```

3. **Создание архива**: 
   ```powershell
   Compress-Archive -Path "dist\*" -DestinationPath "frontend-update-$(Get-Date -Format 'yyyyMMdd-HHmmss').zip" -Force
   ```

4. **Загрузка на сервер**: `scp frontend-update-*.zip root@147.45.161.83:/tmp/`

5. **Полное обновление на сервере**:
   ```bash
   ssh root@147.45.161.83
   cd /var/www/waxhands-app/frontend
   
   # ПОЛНАЯ очистка frontend папки
   rm -rf *
   
   # Распаковка нового архива
   unzip /tmp/frontend-update-*.zip -d .
   
   # Перемещение файлов из dist в корень
   mv dist/* .
   rmdir dist
   
   # Перезагрузка Nginx
   systemctl reload nginx
   ```

#### Критически важные моменты
- **НЕ используй** `rm -rf assets` - это оставляет старые файлы
- **ВСЕГДА используй** `rm -rf *` для полной очистки
- **Проверяй** создание нового JS файла перед деплоем
- **Очищай** все кэши TypeScript перед сборкой

### Альтернативный способ обновления
```bash
ssh root@147.45.161.83
cd /var/www/waxhands-app/backend
git pull origin main
npm run build
pm2 restart waxhands-backend
```

### Процессы и мониторинг
- **Процесс**: Запущен через PM2 как `waxhands-backend`
- **Порт**: 3001 (основной backend)
- **WebSocket сервер**: Отдельный процесс на порту 3002
- **Логи**: `backend.log` в папке backend
- **Администратор**: 
  - **Логин**: Admin
  - **Пароль**: N1fQZhaT$
- **Переменные окружения**:
  ```bash
  HOST=0.0.0.0
  PORT=3001
  DB_HOST=localhost
  DB_PORT=5432
  DB_NAME=waxhands
  DB_USER=waxhands_user
  DB_PASSWORD=waxhands123
  JWT_SECRET=waxhands_jwt_secret_key_2024
  NODE_ENV=production
  UPLOAD_PATH=uploads
  RATE_LIMIT_MAX_UPLOADS=1000
  MAX_FILE_SIZE=50mb
  ```

### Команды мониторинга
```bash
# Проверка процессов
ps aux | grep node
ps aux | grep waxhands-backend

# Просмотр логов
tail -f /var/www/waxhands-app/backend/backend.log
tail -f /var/www/waxhands-app/backend/server.log

# Проверка статуса базы данных
sudo -u postgres psql -d waxhands -c "SELECT version();"
```

### Структура проекта
- **Backend**: TypeScript/Node.js с PostgreSQL
- **Frontend**: React PWA с Vite
- **База данных**: PostgreSQL с пользователем `waxhands_user`
- **Процесс**: Запущен через PM2 как `waxhands-backend`
- **Порт**: 3001 (backend), 3002 (WebSocket), 80/443 (nginx)
- **Домен**: waxhands.ru с SSL сертификатом

### Важные файлы и папки
- **`backend/dist/`**: Скомпилированные файлы бэкенда
- **`backend/uploads/`**: Загруженные файлы пользователей
- **`frontend/assets/`**: Статические ресурсы фронтенда (обновляются при деплое)
- **`frontend/uploads/`**: Загруженные изображения
- **`.env`**: Переменные окружения для production

### Структура фронтенда на сервере
```
/var/www/waxhands-app/frontend/
├── assets/           # Статические ресурсы (JS, CSS, изображения)
├── uploads/          # Загруженные файлы пользователей
└── .env              # Переменные окружения фронтенда
```

**ВАЖНО**: При обновлении фронтенда:
- Файлы из `dist/` должны быть перемещены в корень `frontend/`
- Папка `assets/` должна быть полностью заменена
- Nginx должен быть перезагружен для применения изменений

### Команды для работы с проектом
```bash
# Локальная разработка
npm run dev          # Запуск фронтенда
cd backend && npm run dev  # Запуск бэкенда

# Сборка для production
npm run build        # Сборка фронтенда
cd backend && npm run build  # Сборка бэкенда

# Обновление на сервере
./update-server.ps1  # PowerShell скрипт для backend
# или
ssh root@147.45.161.83 "cd /var/www/waxhands-app/backend && git pull && npm run build && pm2 restart waxhands-backend"

# Обновление фронтенда на сервере
ssh root@147.45.161.83 "cd /var/www/waxhands-app/frontend && rm -rf assets && unzip /tmp/frontend-update.zip -d . && mv dist/* . && rmdir dist && systemctl reload nginx"

# Управление WebSocket сервером
ssh root@147.45.161.83 "cd /var/www/waxhands-app && node websocket-server-simple.js &"
# Проверка статуса WebSocket сервера
ssh root@147.45.161.83 "netstat -tlnp | grep 3002"
```

## Документирование процесса

1. Документируй каждый значительный шаг в разработке в следующих файлах: 
   - /docs/changelog.md - хронологический журнал всех изменений
   - /docs/tasktracker.md - статус выполнения задач с описанием
2. Формат записи в changelog.md: 

   ```javascript
   markdown
   ```

   ```javascript
   ## [YYYY-MM-DD] - Краткое описание изменений
   ### Добавлено
   - Описание новых функций
   
   ### Изменено
   - Описание модификаций
   
   ### Исправлено
   - Описание исправлений
   ```


1. Формат записи в tasktracker.md: 

   ```javascript
   markdown
   ```

   ```javascript
   ## Задача: [Название задачи]
   - **Статус**: [Не начата/В процессе/Завершена]
   - **Описание**: [Детальное описание]
   - **Шаги выполнения**:
     - [x] Завершенный шаг
     - [ ] Текущий шаг
     - [ ] Запланированный шаг
   - **Зависимости**: [Связи с другими задачами]
   ```

## Процесс разработки

 1. Перед началом каждого нового шага спрашивай моего подтверждения.
 2. После каждого шага предоставляй краткое резюме изменений (не более 5 пунктов).
 3. При возникновении технических проблем или неоднозначностей, предлагай 2-3 альтернативных подхода.
 4. Всегда сохраняй контекст текущей задачи и общую цель проекта.
 5. Периодически напоминай о текущем статусе задачи и оставшихся шагах.
 6. Следуй архитектурным решениям и стандартам, описанным в Project.md.
 7. Соблюдай принципы SOLID, KISS, DRY.
 8. Проводи code review для всех изменений.
 9. Используй единый стиль кодирования (линтеры, pre-commit hooks)
10. Не оставляй неиспользуемый код и комментарии.

## Документирование кода и структуры

1. При создании нового файла добавляй в его начало: 

   ```javascript
   /**
    * @file: [имя файла]
    * @description: [краткое описание]
    * @dependencies: [связанные компоненты/файлы]
    * @created: [дата]
    */
   ```
2. После реализации нового функционала актуализируй 

   ```javascript
   /docs/project.md
   ```

   , включая: 
   - Обновленную архитектуру проекта
   - Описание новых компонентов и их взаимодействий
   - При необходимости, диаграммы и схемы в формате Mermaid
3. Поддерживай актуальную документацию API и интерфейсов.

## Коммуникация

1. Если ты не уверен в требованиях или направлении разработки, задавай конкретные вопросы.
2. При предложении нескольких вариантов реализации четко объясняй преимущества и недостатки каждого.
3. Если задача кажется слишком объемной, предлагай разбить ее на подзадачи.
4. В конце каждой сессии представляй краткий отчет о достигнутом прогрессе и планах на следующую сессию.

 
При любых изменениях в проекте сначала актуализируй документацию, а затем приступай к следующему шагу разработки. Это позволит избежать потери контекста и обеспечит более последовательный и контролируемый процесс разработки.

## 🎯 Общие принципы

### Архитектура
- Следуй принципам SOLID, KISS, DRY
- Используй компонентный подход React
- Придерживайся функционального программирования
- Строгая типизация TypeScript обязательна

### Структура проекта
- Компоненты в `src/components/` с подпапками по функциональности
- Страницы в `src/pages/` с ролевой структурой
- Хуки в `src/hooks/` для переиспользуемой логики
- Типы в `src/types/` для TypeScript интерфейсов
- Утилиты в `src/utils/` для вспомогательных функций

## 🎨 Дизайн-система

### Цветовая палитра
- **Primary**: hsl(35 100% 55%) - Яркий оранжевый/желтый
- **Secondary**: hsl(320 100% 60%) - Яркий пурпурный
- **Accent**: hsl(220 100% 65%) - Игривый синий
- **Background**: hsl(45 100% 96%) - Теплый кремовый

### Компоненты
- Используй Shadcn/ui как основу
- Создавай кастомные варианты для специфичных нужд
- Следуй принципам доступности (a11y)
- Адаптивный дизайн обязателен

### Анимации
- Используй кастомные анимации из `tailwind.config.ts`
- Анимации должны быть плавными и ненавязчивыми
- Для детского интерфейса - яркие и игривые анимации

## 🔐 Безопасность

### Аутентификация
- JWT токены для аутентификации
- Защищенные маршруты для каждой роли
- Валидация всех форм на клиенте и сервере
- Хеширование паролей (bcrypt)

### Защита данных
- Соответствие 152-ФЗ для персональных данных
- Шифрование чувствительных данных
- Логирование действий пользователей
- CORS настройки для API

## 👥 Ролевая модель

### Администратор
- Полный доступ к управлению системой
- Интерфейс оптимизирован для десктопа
- Расширенная функциональность и статистика

### Исполнитель
- Доступ к спискам участников
- Упрощенный мобильный интерфейс
- Фокус на выполнении заказов

### Родитель
- Управление профилями детей
- Оплата услуг
- История заказов
- Мобильный интерфейс

### Ребенок
- Упрощенный интерфейс с крупными элементами
- Яркие цвета и анимации
- Ограниченная функциональность
- Подтверждение действий родителем

## 📱 PWA требования

### Функциональность
- Service Worker для кэширования
- Manifest для установки на устройства
- Офлайн функциональность
- Push-уведомления

### Производительность
- First Contentful Paint < 1.5s
- Largest Contentful Paint < 2.5s
- Cumulative Layout Shift < 0.1
- Оптимизация для мобильных устройств

## 🧪 Качество кода

### TypeScript
- Строгая типизация обязательна
- Интерфейсы для всех API ответов
- Типы для всех пропсов компонентов
- Избегай `any` типа

### Предотвращение ошибок TypeError с map
- **ВСЕГДА проверяй** существование массива перед использованием `.map()`:
  ```typescript
  // ❌ НЕПРАВИЛЬНО
  array.map(item => ...)
  
  // ✅ ПРАВИЛЬНО
  (array || []).map(item => ...)
  (array?.items || []).map(item => ...)
  ```

- **Обязательные проверки** для:
  - `masterClass.participants.map()`
  - `masterClass.executors_full.map()`
  - `content.advantages_list.map()`
  - `media.map()`
  - `services.map()`

- **Проверка перед рендерингом**:
  ```typescript
  if (!content) return <div>Загрузка...</div>;
  if (!media || media.length === 0) return <div>Нет данных</div>;
  ```

- **Fallback значения** для всех массивов:
  ```typescript
  const participants = masterClass?.participants || [];
  const executors = masterClass?.executors_full || [];
  ```

### ESLint
- Следуй правилам ESLint
- Исправляй все ошибки перед коммитом
- Используй Prettier для форматирования
- Husky для pre-commit hooks

### Компоненты
- Функциональные компоненты с хуками
- Правильные названия компонентов (PascalCase)
- JSDoc комментарии для сложной логики
- Разделение ответственности

## 📋 Формы и валидация

### Валидация
- React Hook Form для управления формами
- Zod для схем валидации
- Валидация на клиенте и сервере
- Понятные сообщения об ошибках

### UX форм
- Прогрессивное раскрытие полей
- Автосохранение черновиков
- Подтверждение важных действий
- Индикаторы прогресса

## 🔄 Состояние приложения

### React Query
- Для серверного состояния
- Кэширование и инвалидация
- Оптимистичные обновления
- Обработка ошибок

### Локальное состояние
- useState для простого состояния
- useReducer для сложной логики
- Context API для глобального состояния
- Zustand для сложного состояния

## 📊 Данные и API

### Структура данных
- Нормализованная структура
- Типизированные API ответы
- Обработка ошибок
- Логирование запросов

### API эндпоинты
- **Backend**: `/api/master-classes` (НЕ `/api/master-class-events`)
- **Frontend**: Использует `VITE_API_URL` из `.env`
- **WebSocket**: `wss://waxhands.ru/ws` (проксируется на порт 3002)
- **Типы данных**: Все API ответы должны соответствовать TypeScript интерфейсам

### Важные типы данных
- **MasterClassEvent**: Основной интерфейс для мастер-классов
- **MasterClassEventDTO**: DTO для API ответов (включает `executor_names`, `executors_full`, `school_data`)
- **MasterClassStatisticsDTO**: DTO для статистики мастер-классов

### Структура ответа API
```typescript
{
  id: number,
  name: string,
  executor_names: string[],        // Имена исполнителей
  executors_full: Array<{         // Полные данные исполнителей
    id: number,
    name: string,
    surname: string,
    fullName: string
  }>,
  school_data: {                  // Данные школы
    teacher: string,
    teacherPhone: string
  },
  // ... другие поля
}
```

### Кэширование
- Стратегии кэширования React Query
- Локальное кэширование важных данных
- Инвалидация при обновлениях
- Офлайн кэширование

## 🎯 Специфичные требования

### Детский интерфейс
- Крупные элементы управления
- Яркие цвета и анимации
- Простая навигация
- Голосовые подсказки (опционально)
- Ограниченная функциональность

### Мобильная оптимизация
- Touch-friendly интерфейс
- Оптимизация для слабых устройств
- Минимизация трафика
- Быстрая загрузка

### Доступность
- ARIA атрибуты
- Поддержка скринридеров
- Контрастность цветов
- Клавиатурная навигация

## 📝 Документация

### Компоненты
- JSDoc комментарии для всех компонентов
- Описание пропсов и их типов
- Примеры использования
- Ссылки на связанные компоненты

### Проверки перед деплоем
1. **Проверка новых файлов**:
   ```powershell
   # Убедиться, что создался новый JS файл
   dir dist\assets\index-*.js
   # НЕ должен быть старый index-BEWE0pAD.js
   ```

2. **Проверка размера файлов**:
   ```powershell
   # Новый файл должен быть больше старого
   Get-ChildItem dist\assets\index-*.js | Select-Object Name, Length
   ```

3. **Проверка содержимого архива**:
   ```powershell
   # Убедиться, что архив содержит новые файлы
   Expand-Archive -Path "frontend-update-*.zip" -DestinationPath "temp-check" -Force
   dir temp-check\assets\index-*.js
   Remove-Item -Recurse -Force temp-check
   ```

4. **Проверка на сервере после деплоя**:
   ```bash
   ssh root@147.45.161.83 "cd /var/www/waxhands-app/frontend && ls -la assets/ && ls -la assets/index-*.js"
   ```

### API
- Документация всех эндпоинтов
- Примеры запросов и ответов
- Коды ошибок
- Аутентификация

## 🚀 Деплой и мониторинг

### Сборка
- Оптимизация для production
- Code splitting
- Tree shaking
- Минификация

### Правильная последовательность при исправлении ошибок
1. **Исправление кода** в исходных файлах
2. **Полная очистка кэшей**:
   ```powershell
   Remove-Item -Recurse -Force dist
   Remove-Item -Force backend\*.tsbuildinfo
   Remove-Item -Force *.tsbuildinfo
   ```
3. **Пересборка проекта**: `npm run build`
4. **Проверка новых файлов**: убедиться, что создался новый JS файл
5. **Создание архива с timestamp**: `frontend-update-$(Get-Date -Format 'yyyyMMdd-HHmmss').zip`
6. **Полная очистка сервера**: `rm -rf *` вместо `rm -rf assets`
7. **Распаковка и перемещение файлов**
8. **Перезагрузка Nginx**
9. **Проверка успешности обновления**

### Мониторинг
- Логирование ошибок
- Метрики производительности
- Аналитика пользователей
- Мониторинг API

### Отладка и диагностика
- **Проверка API**: Использовать `curl` для тестирования эндпоинтов
- **Логи backend**: `tail -f /var/www/waxhands-app/backend/backend.log`
- **Статус процессов**: `pm2 status` и `ps aux | grep waxhands-backend`
- **Перезапуск при проблемах**: `pm2 restart waxhands-backend`
- **Проверка Nginx**: `systemctl status nginx` и `systemctl reload nginx`
- **Проверка WebSocket**: `netstat -tlnp | grep 3002` и `curl -I -H "Connection: Upgrade" -H "Upgrade: websocket" https://waxhands.ru/ws`

### Частые проблемы и решения
- **"Too many requests"**: Перезапустить backend процесс
- **"Endpoint not found"**: Проверить правильность URL (`/api/master-classes`)
- **Фронтенд не обновляется**: Проверить путь деплоя и перезагрузить Nginx
- **Backend не отвечает**: Проверить статус PM2 и логи
- **WebSocket не работает**: Проверить статус порта 3002 и перезапустить `websocket-server-simple.js`
- **SSL ошибки**: Проверить пути сертификатов в Nginx конфигурации

### Диагностика проблем с обновлением Frontend
- **Старый JS файл загружается**: 
  ```bash
  # Проверить на сервере
  ssh root@147.45.161.83 "cd /var/www/waxhands-app/frontend && ls -la assets/"
  
  # Если видишь старый index-BEWE0pAD.js вместо нового - проблема в деплое
  ```

- **Кэширование браузера**: 
  ```bash
  # Принудительная очистка кэша
  ssh root@147.45.161.83 "cd /var/www/waxhands-app/frontend && rm -rf * && unzip /tmp/frontend-update-*.zip -d . && mv dist/* . && rmdir dist"
  ```

- **Локальная сборка не обновляется**:
  ```powershell
  # Полная очистка кэшей
  Remove-Item -Recurse -Force dist
  Remove-Item -Force backend\*.tsbuildinfo
  Remove-Item -Force *.tsbuildinfo
  npm run build
  ```

- **Проверка успешности обновления**:
  ```bash
  # На сервере
  ssh root@147.45.161.83 "cd /var/www/waxhands-app/frontend && ls -la assets/ && systemctl status nginx"
  
  # В браузере - проверить Network tab на новый JS файл
  ```

### Решение проблем с Backend Uploads (404 ошибки и CORS)
- **Проблема**: Nginx ищет backend uploads в frontend папке, CORS ошибки при доступе с фронтенда
- **Решение**: Использовать отдельный порт 8080 для backend uploads с правильными CORS заголовками
- **Конфигурация**:
  ```bash
  # Создать отдельный конфиг для backend uploads с CORS
  echo 'server { listen 8080; location /uploads/ { alias /var/www/waxhands-app/backend/uploads/; add_header Access-Control-Allow-Origin "*"; add_header Access-Control-Allow-Methods "GET, OPTIONS"; add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range"; if ($request_method = OPTIONS) { add_header Access-Control-Allow-Origin "*"; add_header Access-Control-Allow-Methods "GET, OPTIONS"; add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range"; add_header Content-Length 0; add_header Content-Type text/plain; return 204; } } }' > /etc/nginx/sites-available/backend-uploads
  
  # Включить конфиг
  ln -s /etc/nginx/sites-available/backend-uploads /etc/nginx/sites-enabled/
  
  # Перезагрузить Nginx
  nginx -t && systemctl reload nginx
  ```
- **URL для backend uploads**: `http://147.45.161.83:8080/uploads/` (включая /uploads/ в пути)
- **CORS заголовки**: Настроены для всех типов файлов с поддержкой preflight OPTIONS запросов
- **Проверка работы**:
  ```bash
  # Проверить CORS preflight
  curl -X OPTIONS -H "Origin: http://147.45.161.83" -H "Access-Control-Request-Method: GET" http://147.45.161.83:8080/uploads/images/images-*.jpg
  
  # Проверить изображение с CORS
  curl -H "Origin: http://147.45.161.83" -I http://147.45.161.83:8080/uploads/images/images-*.jpg
  
  # Проверить видео с CORS
  curl -H "Origin: http://147.45.161.83" -I http://147.45.161.83:8080/uploads/videos/videos-*.mp4
  ```

### WebSocket URL - КРИТИЧЕСКИ ВАЖНО
- **Правильный URL**: `wss://waxhands.ru:3002/ws` (с портом 3002)
- **Неправильный URL**: `wss://waxhands.ru/api/chat/ws` (НЕ использовать)
- **Проверка в .env файлах**:
  ```bash
  # Должно быть:
  VITE_WS_URL=wss://waxhands.ru:3002/ws
  
  # НЕ должно быть:
  VITE_WS_URL=wss://waxhands.ru/api/chat/ws
  ```
- **Запрещено**: Использовать `/api/chat/ws` или другие WebSocket пути
- **Единственный**: WebSocket сервер должен работать только на порту 3002 с путем `/ws`

---

**Последнее обновление:** 2025-08-25
**Обновлено:** Добавлена информация о домене waxhands.ru, SSL сертификате, отдельном WebSocket сервере на порту 3002, процессе развертывания фронтенда, структуре API, отладке, предотвращении ошибок TypeError с map и решении CORS проблем для backend uploads
**Ответственный:** Алексей 